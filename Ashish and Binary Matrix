#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,m;
        set<string> st;
        cin>>n>>m;
        for(int i=0;i<n;i++)
        {
            string s;
            cin>>s;
            st.insert(s);
        }
        if(st.size()==n)
           cout<<"Yes"<<"\n";
        else
          cout<<"No"<<"\n";
    }
}
