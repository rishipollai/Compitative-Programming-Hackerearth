#include<bits/stdc++.h>
using namespace std;
#define ll long long 
int main()
{
    ll int i,n,m,s=0;
    cin>>n>>m;
    ll int a[n];
    for(i=0;i<n;i++) cin>>a[i];
    for(i=0;i<n;i++)
    {
        if(a[i]==m)
             s=i;
    }
    if(s==0)
      cout<<"-1";
    else
      cout<<s+1;

}
