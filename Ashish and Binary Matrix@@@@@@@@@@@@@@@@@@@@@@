#include <bits/stdc++.h>
#define ll long long int
//#define rep(i,a,b) for(ll i=a;i<b;i++)
using namespace std;
 
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    set<string> v;
    cin>>t;
    while(t--)
    {
        ll n,m,flag=0;
        cin>>n>>m;
        string s;
        for(ll i=0;i<n;i++)
        {
            cin>>s;
            v.insert(s);
        }
        if(v.size()==n)
            cout<<"Yes\n";
        else
            cout<<"No\n";
        v.clear();
    }
    return 0;
}
