#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        string s,p;
        cin>>s;
        p=s;
        reverse(s.begin(),s.end());
        if(p==s)
          cout<<"YES\n";
        else
         cout<<"NO\n";

    }
}
