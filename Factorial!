#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,c=1,i;
    cin>>n;
    if(n==0)
       cout<<"1";
    else
    {
        for(i=1;i<=n;i++)
         c=c*i;
        cout<<c;
    }
}
