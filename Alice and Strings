#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    string s1,s2;
    // vector<int>v;
    cin>>s1;
    cin>>s2;
    int f=0;
    ll n=s1.length();
    if(s1.length()==s2.length())
    {
        for(ll i=0;i<n;i++)
        {
            if(s1[i]>s2[i])
            {
                f=1;
                break;
            }
        }
        if(f==1)
        {
            cout<<"NO"<<endl;
        }
        else{
            cout<<"YES"<<endl;
        }
    }
    else{
        cout<<"NO"<<endl;
    }
    return 0;
}Alice and Strings
